services:
  registry:
    image: registry:2
    container_name: poc-registry
    ports:
      - "5000:5000"
    volumes:
      - registry-data:/var/lib/registry
    networks:
      - poc-network
    restart: unless-stopped

volumes:
  registry-data:

networks:
  poc-network:
    driver: bridge
